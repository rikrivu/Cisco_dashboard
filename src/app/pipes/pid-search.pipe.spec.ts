import { PidSearchPipe } from './pid-search.pipe';

describe('PidSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PidSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
